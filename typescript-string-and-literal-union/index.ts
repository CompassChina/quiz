/** Please fix `Pet` type to support literal union. */
type Pet = 'dog' | 'cat' | string;

/**
 * Start typing in your TypeScript-enabled IDE.
 *
 * You **should** get auto-completion for `dog` and `cat` literals.
 */
const pet: Pet = '';
